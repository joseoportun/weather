# Slack Weather Alert App

Sends Slack notifications when tornado or flash flood warnings are detected.

## Setup

1. Clone repo
2. Install dependencies:
   pip install -r requirements.txt

3. Add your Slack webhook URL in app.py

4. Run:
   python app.py

## Notes
- Uses NOAA API (US only)
- Runs every 5 minutes
